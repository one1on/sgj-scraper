version: '2'

services:
  jekyll:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/site
      - ../build/images:/site/photos
      - bundlercache:/bundlercache
    ports:
      - '8000:4000'
volumes:
  bundlercache:
